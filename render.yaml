services:
  - type: web
    name: weather-frontend
    runtime: node
    buildCommand: cd frontend && npm install && npm run build
    startCommand: cd frontend && npm start -- -p $PORT
    envVars:
      - key: NEXT_PUBLIC_API_URL
        value: https://house-temp-data.onrender.com
